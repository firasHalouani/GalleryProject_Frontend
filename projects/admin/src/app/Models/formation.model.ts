export class Formation {
  constructor(
      private id?: number,
      private name?: string,
      private description?: string,
      private formateur?: string,
      private debut?: string,
      private fin?: string,
      private heures?: number,
      private prix?: number,
      private image?: string
  ) {}
}
